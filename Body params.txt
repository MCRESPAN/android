En el back:
- Hacerlo post, quitar de la url /:id
- const { categoria_id } = req.body; //Manera de recuperar la info

Front:
- En el ApiService: Hacerlo POST y (@Body CategoriaRequest request);
- Hacer la clase CategoriaRequest con la info que se quiere mandar
- En la llamada crear un objeto de esa nueva clase con la info en el constructor y mandarlo como argumento a la función del Apiservice

CategoriaRequest request = new CategoriaRequest(categoriaId); // Nuevo
Call<List<Productos>> call = apiService.getProductosPorCategoria(request);